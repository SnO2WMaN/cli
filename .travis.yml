language: node_js
node_js:
    - '10'
    - '8'
cache:
    yarn: true
    directories:
        - 'node_modules'
install:
    - yarn install --ignore-engines
    - yarn global add flow-typed
before_script:
    - flow-typed install
script:
    - commitlint-travis
    - yarn build
    - yarn test
